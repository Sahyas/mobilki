rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /books/{bookId} {
      allow read, write: if true;
    }

    match /books/{bookId}/dynamicMetadata/{metaId} {
      allow read, write: if true;
    }
  }
}
